protocol_version: "0.3"
profile: micro

project_id:
agent_id:
task_version:
updated_at:
status: READY

objective:

inputs: []

allowed_changes: []

expected_outputs: []

acceptance: {}

validation: []

depends_on: []

# Authoritative files whose stable defaults apply; explicit task fields override them.
inherits: []

return_path: "HANDOFFS/[agent_id].yaml"
